from setuptools import setup

version = '1.0.0'

package_dir = 'dbman'

setup(name='dbman',

    version=version,

    description='I/O Interface to Pure Python Database Driver',

    packages=[package_dir, ],
)
